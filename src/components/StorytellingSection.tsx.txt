import { motion, useInView } from "framer-motion";
import { useRef } from "react";
import { Sparkles, Eye, Brain } from "lucide-react";

const StorytellingSection = () => {
  const ref = useRef(null);
  const isInView = useInView(ref, { once: true, margin: "-100px" });

  return (
    <section ref={ref} className="section-padding relative overflow-hidden">
      <div className="absolute inset-0 hero-gradient opacity-50" />

      <div className="relative z-10 max-w-6xl mx-auto">
        <motion.div
          className="text-center mb-16"
          initial={{ opacity: 0, y: 40 }}
          animate={isInView ? { opacity: 1, y: 0 } : {}}
          transition={{ duration: 0.8 }}
        >
          <p className="text-primary/70 font-body text-sm tracking-[0.3em] uppercase mb-4">
            L'Expérience
          </p>
          <h2 className="text-3xl md:text-5xl font-display font-bold text-gradient-gold mb-6">
            Au-delà du Spectacle
          </h2>
          <p className="text-muted-foreground text-lg max-w-2xl mx-auto leading-relaxed">
            Imaginez un instant où le temps s'arrête. Où vos invités ne sont plus spectateurs,
            mais acteurs d'une expérience qui dépasse l'entendement.
          </p>
        </motion.div>

        <div className="grid md:grid-cols-3 gap-8">
          {[
            {
              icon: <Eye className="w-8 h-8" />,
              title: "Hypnose",
              description:
                "Un voyage intérieur spectaculaire. Vos invités vivent des moments de transe fascinants, entre rire et émerveillement.",
            },
            {
              icon: <Brain className="w-8 h-8" />,
              title: "Mentalisme",
              description:
                "Lecture de pensées, prédictions impossibles, synchronicités troublantes. Le mental n'a plus de secrets.",
            },
            {
              icon: <Sparkles className="w-8 h-8" />,
              title: "Émotion",
              description:
                "Chaque spectacle est conçu pour créer un souvenir inoubliable. Une expérience collective qui soude et émerveille.",
            },
          ].map((item, i) => (
            <motion.div
              key={item.title}
              className="glass-card-hover p-8 text-center"
              initial={{ opacity: 0, y: 30 }}
              animate={isInView ? { opacity: 1, y: 0 } : {}}
              transition={{ duration: 0.6, delay: 0.2 + i * 0.15 }}
            >
              <div className="inline-flex items-center justify-center w-16 h-16 rounded-full bg-primary/10 text-primary mb-6">
                {item.icon}
              </div>
              <h3 className="text-xl font-display font-semibold text-foreground mb-3">
                {item.title}
              </h3>
              <p className="text-muted-foreground leading-relaxed text-sm">
                {item.description}
              </p>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
};

export default StorytellingSection;
